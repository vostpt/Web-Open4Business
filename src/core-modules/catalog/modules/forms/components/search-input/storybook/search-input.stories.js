import { SearchInputComponent } from "../search-input.component";
import notes from './search-input.md';

export default {
  title: 'Form search input',
  parameters: {
    notes: { notes },
  }
}

export const usageNotes = () => ({
  component: SearchInputComponent
});
